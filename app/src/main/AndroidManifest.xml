<?xml version="1.0" encoding="utf-8" standalone="no"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.winlator">
    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- DGPlayer bridge: only apps signed with the same certificate (i.e. dsam3) may launch games. -->
    <permission
        android:name="com.retrople.permission.LAUNCH_GAME"
        android:protectionLevel="signature"/>

    <application
        android:icon="@mipmap/ic_launcher"
        android:appCategory="game"
        android:isGame="true"
        android:extractNativeLibs="true"
        android:allowAudioPlaybackCapture="true"
        android:label="@string/app_name">

        <activity android:name="com.winlator.MainActivity"
            android:theme="@style/AppThemeDark"
            android:exported="true"
            android:screenOrientation="sensor"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|smallestScreenSize|density|navigation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name="com.winlator.XServerDisplayActivity"
            android:exported="false"
            android:theme="@style/AppThemeFullscreenDark"
            android:supportsPictureInPicture="true"
            android:launchMode="singleTask"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|smallestScreenSize|density|navigation"
            android:screenOrientation="sensorLandscape" />

        <!--
            DGPlayer bridge entry point. XServerDisplayActivity is exported="false" upstream and
            MainActivity cannot start a game, so this is the only way an external app can launch one.
            Caller identity is enforced in code (signature match, see GameLaunchActivity.isCallerTrusted)
            rather than via android:permission, so the contract stays testable with `adb shell am start`.
            Release hardening: also add android:permission="com.retrople.permission.LAUNCH_GAME".
        -->
        <activity android:name="com.winlator.bridge.GameLaunchActivity"
            android:exported="true"
            android:theme="@style/AppThemeDark"
            android:excludeFromRecents="true"
            android:screenOrientation="sensorLandscape"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|smallestScreenSize|density|navigation">
            <intent-filter>
                <action android:name="com.retrople.action.PLAY_GAME"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>

        <!--
            Lends ControlsEditorActivity to DGPlayer. That activity stays exported="false" - only this
            bridge may reach it, and only from a caller that passes BridgeSecurity.isCallerTrusted.
            sensorLandscape matches both ControlsEditorActivity and XServerDisplayActivity so the
            layout is authored in exactly the orientation the game will run in.
        -->
        <activity android:name="com.winlator.bridge.ControlsEditActivity"
            android:exported="true"
            android:theme="@style/AppThemeDark"
            android:excludeFromRecents="true"
            android:screenOrientation="sensorLandscape"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|smallestScreenSize|density|navigation">
            <intent-filter>
                <action android:name="com.retrople.action.EDIT_CONTROLS"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>

        <!--
            Two-way sync of the controls-profile list with DGPlayer. Invisible: it finishes inside
            onCreate under a translucent theme. Caller is checked by BridgeSecurity.
        -->
        <activity android:name="com.winlator.bridge.ControlsSyncActivity"
            android:exported="true"
            android:theme="@style/AppThemeTransparent"
            android:excludeFromRecents="true"
            android:screenOrientation="sensorLandscape"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|smallestScreenSize|density|navigation">
            <intent-filter>
                <action android:name="com.retrople.action.SYNC_CONTROLS"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>

        <activity android:name="com.winlator.ControlsEditorActivity"
            android:exported="false"
            android:theme="@style/AppThemeFullscreenDark"
            android:screenOrientation="sensorLandscape" />

        <activity android:name="com.winlator.ExternalControllerBindingsActivity"
            android:theme="@style/AppThemeDark"
            android:exported="false"
            android:screenOrientation="sensor" />

        <!-- Needed for keep the container alive when in background or the device is locked. -->
        <service
            android:name="com.winlator.services.ForegroundService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback"
            android:stopWithTask="false">
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.FileProvider"
            android:exported="false"
            android:grantUriPermissions="true">

            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"/>
        </provider>
    </application>
</manifest>
